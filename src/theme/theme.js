import { createTheme } from "@material-ui/core/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#AB47BC",
    },
    secondary: {
      main: "#7986cb",
    },
    warnings: {
      main: "ff9800",
    },
    success: {
      main: "#aed581",
    },
  },
});
